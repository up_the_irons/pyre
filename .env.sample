# Path to the pyre database file
PYRE_DB_PATH=./pyre.db

# Company metadata (name shown in PDF reports and app title) is read from
# a company.yaml file in the same directory as the database. See company.yaml.sample.
